# pjfm Web Application Dependencies
flask>=2.0.0
flask-socketio>=5.0.0
python-socketio>=5.0.0
python-engineio>=4.0.0
eventlet>=0.30.0
opuslib>=3.0.0

# System dependencies:
# sudo apt install libopus0 libopus-dev

# Also requires dependencies from parent pjfm project:
# numpy
# scipy
# (and the SignalHound BB API library)
